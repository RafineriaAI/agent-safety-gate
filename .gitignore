# Build and environment
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tool caches
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Records, approvals and demo output produced by running the gate
.agent-safety-gate/

# A trace derived from a real session transcript. It is session data, so it
# stays on the machine that produced it; benchmarks/README.md explains how to
# make your own.
benchmarks/traces/real_session.jsonl
